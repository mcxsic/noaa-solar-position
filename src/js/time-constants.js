let SECONDS_TO_MILLIS = 1000;
let MINUTES_TO_SECONDS = 60;
let MINUTES_TO_MILLIS = MINUTES_TO_SECONDS * SECONDS_TO_MILLIS;
let HOURS_TO_MINUTES = 60;
let HOURS_TO_SECONDS = HOURS_TO_MINUTES * MINUTES_TO_SECONDS;
let HOURS_TO_MILLIS = HOURS_TO_SECONDS * SECONDS_TO_MILLIS;
let DAYS_TO_HOURS = 24;
let DAYS_TO_MILLIS = DAYS_TO_HOURS * HOURS_TO_MILLIS;

module.exports = {
  SECONDS_TO_MILLIS,
  MINUTES_TO_SECONDS,
  MINUTES_TO_MILLIS,
  HOURS_TO_MINUTES,
  HOURS_TO_SECONDS,
  HOURS_TO_MILLIS,
  DAYS_TO_HOURS,
  DAYS_TO_MILLIS,
};
